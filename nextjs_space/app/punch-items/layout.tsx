export const metadata = {
  title: 'Punch List | SiteSync OS',
  description: 'Manage construction project punch items',
};

export default function PunchItemsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
