export const metadata = {
  title: 'Submittals | SiteSync OS',
  description: 'Manage construction submittals',
};

export default function SubmittalsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
