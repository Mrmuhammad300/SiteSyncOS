export const metadata = {
  title: 'Change Orders | STRATOS',
  description: 'Manage construction project change orders',
};

export default function ChangeOrdersLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
